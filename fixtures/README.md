# Fixtures

`sample-session.json` is a short fictional successful session ("add retry to fetch wrapper").

Expected after convert / demo:

- `skills/add-retry-to-fetch/SKILL.md`
- `skills/add-retry-to-fetch/INSTALL.md`
- `skills/add-retry-to-fetch/references/session-summary.md`
